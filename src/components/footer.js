import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => (
  <footer className="footer">
  </footer>
);

Footer.propTypes = {
  dark: PropTypes.bool
};

export { Footer };
